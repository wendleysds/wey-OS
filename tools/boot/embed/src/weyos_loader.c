#include <headers.h>
#include <file.h>
#include <utils.h>
#include <def/err.h>

int weyos_loader(struct file* file, void** out_entry_point, void* initrd_addr){
	/*char buffer[4096];

	file->ops->read(file, buffer, 0x200);

	struct boot_header* boot_header = (struct boot_header*)buffer;*/

	return NOT_IMPLEMENTED;
}
